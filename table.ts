function mul(number:number){
    for(let i =1;i<=10;i++){
        const result = i*number;
        console.log(`${number} * ${i} = ${result}`);
    }
}
mul(2);
console.log("**************");

function multiply(nu:number){
    for(let j=1;j<=10;j++){
        const resu = j*nu;
    
    console.log(`${nu} * ${j} = ${resu}`);
}
}
multiply(3);